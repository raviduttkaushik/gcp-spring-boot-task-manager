spring.application.name=taskmanagerdemo
# Listen on all network interfaces, not just localhost
server.address=0.0.0.0
# Use the port Cloud Run gives us, or default to 8080 locally
server.port=${PORT:8080}
